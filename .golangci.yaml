version: "2"

run:
  timeout: 5m
  relative-path-mode: gomod

linters:
  default: standard
  enable:
    - gosec
  settings:
    gosec:
      excludes:
        - G304 # file path from variable is expected for a CLI
